# ⚡ БЫСТРЫЙ СТАРТ (3 МИНУТЫ)

## Вариант 1: На одной машине (локально)

```bash
# Шаг 1: Собрать
mvn clean package

# Шаг 2: Запустить сервер (терминал 1)
java -cp "target/jade-delivery-system.jar;lib/*" server.ServerLauncher

# Шаг 3: Запустить клиент (терминал 2, через 3 сек)
java -cp "target/jade-delivery-system.jar;lib/*" client.ClientLauncher localhost

# Шаг 4: Проверить результаты
cat output/schedule.csv
```

**Ожидаемый вывод:** Файлы `output/schedule.csv` и `output/schedule.xlsx`

---

## Вариант 2: На двух машинах

**Машина 1 (192.168.1.100) — Сервер:**
```bash
mvn clean package
java -cp "target/jade-delivery-system.jar;lib/*" server.ServerLauncher 1099
```

**Машина 2 — Клиент:**
```bash
java -cp "target/jade-delivery-system.jar;lib/*" client.ClientLauncher 192.168.1.100 1099 Client2
```

---

## Структура файлов для копирования

```
jade-delivery-system/
├── pom.xml                          # Собрать один раз
├── data/
│   ├── products.csv
│   ├── stores.csv
│   └── trucks.csv
├── src/
│   └── main/java/com/multiagent/
│       ├── agents/       (4 файла)
│       ├── model/        (5 файлов)
│       ├── io/           (2 файла)
│       ├── planning/     (1 файл)
│       ├── util/         (1 файл)
│       ├── server/       (1 файл)
│       └── client/       (1 файл)
└── output/               (создаётся автоматически)
```

---

## ✅ Чек-лист

- [ ] Java 11+ установлена (`java -version`)
- [ ] Maven установлен (`mvn -version`)
- [ ] Проект скопирован и собран
- [ ] Файлы `data/*.csv` присутствуют
- [ ] Сервер запущен
- [ ] Клиент подключился к серверу
- [ ] Файлы в `output/` созданы

---

## 🔧 Если что-то не работает

```bash
# Проверить Java
java -version

# Собрать заново
mvn clean package -DskipTests

# Очистить
mvn clean

# Использовать другой порт (если 1099 занят)
java -cp "target/jade-delivery-system.jar;lib/*" server.ServerLauncher 1100
```

---

## 📊 Результаты

После запуска в папке `output/` будут:

- **schedule.csv** — текстовый формат расписания
- **schedule.xlsx** — Excel файл с форматированием

Содержит:
- Какой грузовик везёт какие товары
- Во сколько приезжает в магазин
- Общая стоимость доставки
- Общее расстояние

---

**Готово! Проект работает. Подробнее см. README.md**
